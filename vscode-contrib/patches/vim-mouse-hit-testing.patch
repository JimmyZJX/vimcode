diff --git a/src/vs/editor/browser/controller/mouseHandler.ts b/src/vs/editor/browser/controller/mouseHandler.ts
--- a/src/vs/editor/browser/controller/mouseHandler.ts
+++ b/src/vs/editor/browser/controller/mouseHandler.ts
@@ -8,11 +8,12 @@ import { StandardWheelEvent, IMouseWheelEvent } from '../../../base/browser/mous
 import { Disposable, IDisposable } from '../../../base/common/lifecycle.js';
 import * as platform from '../../../base/common/platform.js';
 import { HitTestContext, MouseTarget, MouseTargetFactory, PointerHandlerLastRenderData } from './mouseTarget.js';
-import { IMouseTarget, IMouseTargetViewZoneData, MouseTargetType } from '../editorBrowser.js';
+import { IMouseTarget, IMouseTargetContentEmpty, IMouseTargetContentText, IMouseTargetViewZoneData, MouseTargetType } from '../editorBrowser.js';
 import { ClientCoordinates, EditorMouseEvent, EditorMouseEventFactory, GlobalEditorPointerMoveMonitor, createEditorPagePosition, createCoordinatesRelativeToEditor } from '../editorDom.js';
 import { ViewController } from '../view/viewController.js';
 import { EditorZoom } from '../../common/config/editorZoom.js';
 import { Position } from '../../common/core/position.js';
+import { Range } from '../../common/core/range.js';
 import { Selection } from '../../common/core/selection.js';
 import { HorizontalPosition } from '../view/renderingContext.js';
 import { ViewContext } from '../../common/viewModel/viewContext.js';
@@ -641,6 +642,7 @@ class MouseDownOperation extends Disposable {
 		this._viewController.dispatchMouse({
 			position: position.position,
 			mouseColumn: position.mouseColumn,
+			vimMouseCharacterViewRange: this._vimMouseCharacterViewRange(position, inSelectionMode),
 			startedOnLineNumbers: this._mouseState.startedOnLineNumbers,
 			revealType,
 
@@ -657,6 +659,49 @@ class MouseDownOperation extends Disposable {
 			onInjectedText: position.type === MouseTargetType.CONTENT_TEXT && position.detail.injectedText !== null
 		});
 	}
+
+	/**
+	 * VSCode's mouse target position is a cursor boundary. Vim block-cursor selection
+	 * needs the character cell under the pointer. During drag, VSCode can report an
+	 * empty CONTENT_TEXT range at boundaries or CONTENT_EMPTY after line text; synthesize
+	 * the adjacent cell so selection extension is stable and includes EOL characters.
+	 */
+	private _vimMouseCharacterViewRange(position: IMouseTarget, inSelectionMode: boolean): Range | undefined {
+		if (!this._isVimCharacterModeEnabled()) {
+			return undefined;
+		}
+		if (position.type === MouseTargetType.CONTENT_TEXT) {
+			return position.range.isEmpty()
+				? this._vimSyntheticCharacterViewRange(position, inSelectionMode)
+				: position.range;
+		}
+		if (position.type === MouseTargetType.CONTENT_EMPTY) {
+			return this._vimSyntheticCharacterViewRange(position, inSelectionMode);
+		}
+		return undefined;
+	}
+
+	private _vimSyntheticCharacterViewRange(position: IMouseTargetContentText | IMouseTargetContentEmpty, inSelectionMode: boolean): Range | undefined {
+		if (!inSelectionMode
+			|| (position.type === MouseTargetType.CONTENT_EMPTY && position.detail.isAfterLines)) {
+			return undefined;
+		}
+
+		const lineNumber = position.position.lineNumber;
+		const column = position.position.column;
+		const lineMaxColumn = this._context.viewModel.getLineMaxColumn(lineNumber);
+		if (column < lineMaxColumn) {
+			return new Range(lineNumber, column, lineNumber, column + 1);
+		}
+		if (column > 1) {
+			return new Range(lineNumber, column - 1, lineNumber, column);
+		}
+		return undefined;
+	}
+
+	private _isVimCharacterModeEnabled(): boolean {
+		return this._viewHelper.viewDomNode.closest('.vim-character-mode-enabled') !== null;
+	}
 }
 
 class MouseDownState {
diff --git a/src/vs/editor/browser/view/viewController.ts b/src/vs/editor/browser/view/viewController.ts
--- a/src/vs/editor/browser/view/viewController.ts
+++ b/src/vs/editor/browser/view/viewController.ts
@@ -8,6 +8,7 @@ import { CoreNavigationCommands, NavigationCommandRevealType } from '../coreComm
 import { IEditorMouseEvent, IPartialEditorMouseEvent } from '../editorBrowser.js';
 import { ViewUserInputEvents } from './viewUserInputEvents.js';
 import { Position } from '../../common/core/position.js';
+import { Range } from '../../common/core/range.js';
 import { Selection } from '../../common/core/selection.js';
 import { IEditorConfiguration } from '../../common/config/editorConfiguration.js';
 import { IViewModel } from '../../common/viewModel.js';
@@ -23,6 +24,8 @@ export interface IMouseDispatchData {
 	 * Desired mouse column (e.g. when position.column gets clamped to text length -- clicking after text on a line).
 	 */
 	mouseColumn: number;
+	/** Mouse-only: view range of the character cell under the pointer in Vim block-cursor mode. */
+	vimMouseCharacterViewRange?: Range;
 	revealType: NavigationCommandRevealType;
 	startedOnLineNumbers: boolean;
 
@@ -237,10 +240,10 @@ export class ViewController {
 		} else if (data.mouseDownCount === 2) {
 			if (!data.onInjectedText) {
 				if (this._hasMulticursorModifier(data)) {
-					this._lastCursorWordSelect(data.position, data.revealType);
+					this._lastCursorWordSelect(data.position, data.revealType, data.vimMouseCharacterViewRange);
 				} else {
 					if (data.inSelectionMode) {
-						this._wordSelectDrag(data.position, data.revealType);
+						this._wordSelectDrag(data.position, data.revealType, data.vimMouseCharacterViewRange);
 					} else {
 						const model = this.viewModel.model;
 						const modelPos = this._convertViewToModelPosition(data.position);
@@ -248,7 +251,7 @@ export class ViewController {
 						if (selection) {
 							this._select(selection);
 						} else {
-							this._wordSelect(data.position, data.revealType);
+							this._wordSelect(data.position, data.revealType, data.vimMouseCharacterViewRange);
 						}
 					}
 				}
@@ -275,7 +278,7 @@ export class ViewController {
 						if (columnSelection) {
 							this._columnSelect(data.position, data.mouseColumn, true);
 						} else {
-							this._moveToSelect(data.position, data.revealType);
+							this._moveToSelect(data.position, data.revealType, data.vimMouseCharacterViewRange);
 						}
 					}
 				} else {
@@ -285,12 +288,15 @@ export class ViewController {
 		}
 	}
 
-	private _usualArgs(viewPosition: Position, revealType: NavigationCommandRevealType): CoreNavigationCommands.MoveCommandOptions {
+	private _usualArgs(viewPosition: Position, revealType: NavigationCommandRevealType, vimMouseCharacterViewRange?: Range): CoreNavigationCommands.MoveCommandOptions {
 		viewPosition = this._validateViewColumn(viewPosition);
 		return {
 			source: 'mouse',
 			position: this._convertViewToModelPosition(viewPosition),
 			viewPosition,
+			vimMouseCharacterModelRange: vimMouseCharacterViewRange === undefined
+				? undefined
+				: this.viewModel.coordinatesConverter.convertViewRangeToModelRange(vimMouseCharacterViewRange),
 			revealType
 		};
 	}
@@ -299,8 +305,8 @@ export class ViewController {
 		CoreNavigationCommands.MoveTo.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType));
 	}
 
-	private _moveToSelect(viewPosition: Position, revealType: NavigationCommandRevealType): void {
-		CoreNavigationCommands.MoveToSelect.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType));
+	private _moveToSelect(viewPosition: Position, revealType: NavigationCommandRevealType, vimMouseCharacterViewRange?: Range): void {
+		CoreNavigationCommands.MoveToSelect.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType, vimMouseCharacterViewRange));
 	}
 
 	private _columnSelect(viewPosition: Position, mouseColumn: number, doColumnSelect: boolean): void {
@@ -328,16 +334,16 @@ export class ViewController {
 		CoreNavigationCommands.LastCursorMoveToSelect.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType));
 	}
 
-	private _wordSelect(viewPosition: Position, revealType: NavigationCommandRevealType): void {
-		CoreNavigationCommands.WordSelect.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType));
+	private _wordSelect(viewPosition: Position, revealType: NavigationCommandRevealType, vimMouseCharacterViewRange?: Range): void {
+		CoreNavigationCommands.WordSelect.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType, vimMouseCharacterViewRange));
 	}
 
-	private _wordSelectDrag(viewPosition: Position, revealType: NavigationCommandRevealType): void {
-		CoreNavigationCommands.WordSelectDrag.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType));
+	private _wordSelectDrag(viewPosition: Position, revealType: NavigationCommandRevealType, vimMouseCharacterViewRange?: Range): void {
+		CoreNavigationCommands.WordSelectDrag.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType, vimMouseCharacterViewRange));
 	}
 
-	private _lastCursorWordSelect(viewPosition: Position, revealType: NavigationCommandRevealType): void {
-		CoreNavigationCommands.LastCursorWordSelect.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType));
+	private _lastCursorWordSelect(viewPosition: Position, revealType: NavigationCommandRevealType, vimMouseCharacterViewRange?: Range): void {
+		CoreNavigationCommands.LastCursorWordSelect.runCoreEditorCommand(this.viewModel, this._usualArgs(viewPosition, revealType, vimMouseCharacterViewRange));
 	}
 
 	private _lineSelect(viewPosition: Position, revealType: NavigationCommandRevealType): void {
diff --git a/src/vs/editor/browser/coreCommands.ts b/src/vs/editor/browser/coreCommands.ts
--- a/src/vs/editor/browser/coreCommands.ts
+++ b/src/vs/editor/browser/coreCommands.ts
@@ -374,6 +374,8 @@ export namespace CoreNavigationCommands {
 	export interface MoveCommandOptions extends BaseCommandOptions {
 		position: IPosition;
 		viewPosition?: IPosition;
+		/** Mouse-only: model range of the character cell under the pointer in Vim block-cursor mode. */
+		vimMouseCharacterModelRange?: Range;
 		revealType: NavigationCommandRevealType;
 	}
 
@@ -395,7 +397,7 @@ export namespace CoreNavigationCommands {
 				args.source,
 				CursorChangeReason.Explicit,
 				[
-					CursorMoveCommands.moveTo(viewModel, viewModel.getPrimaryCursorState(), this._inSelectionMode, args.position, args.viewPosition)
+					CursorMoveCommands.moveTo(viewModel, viewModel.getPrimaryCursorState(), this._inSelectionMode, args.position, args.viewPosition, args.vimMouseCharacterModelRange)
 				]
 			);
 			if (cursorStateChanged && args.revealType !== NavigationCommandRevealType.None) {
@@ -1646,7 +1648,7 @@ export namespace CoreNavigationCommands {
 				args.source,
 				CursorChangeReason.Explicit,
 				[
-					CursorMoveCommands.word(viewModel, viewModel.getPrimaryCursorState(), this._inSelectionMode, args.position)
+					CursorMoveCommands.word(viewModel, viewModel.getPrimaryCursorState(), this._inSelectionMode, args.position, args.vimMouseCharacterModelRange)
 				]
 			);
 			if (args.revealType !== NavigationCommandRevealType.None) {
@@ -1684,7 +1686,7 @@ export namespace CoreNavigationCommands {
 			const states = viewModel.getCursorStates();
 			const newStates: PartialCursorState[] = states.slice(0);
 			const lastAddedState = states[lastAddedCursorIndex];
-			newStates[lastAddedCursorIndex] = CursorMoveCommands.word(viewModel, lastAddedState, lastAddedState.modelState.hasSelection(), args.position);
+			newStates[lastAddedCursorIndex] = CursorMoveCommands.word(viewModel, lastAddedState, lastAddedState.modelState.hasSelection(), args.position, args.vimMouseCharacterModelRange);
 
 			viewModel.model.pushStackElement();
 			viewModel.setCursorStates(
diff --git a/src/vs/editor/common/cursor/cursorMoveCommands.ts b/src/vs/editor/common/cursor/cursorMoveCommands.ts
--- a/src/vs/editor/common/cursor/cursorMoveCommands.ts
+++ b/src/vs/editor/common/cursor/cursorMoveCommands.ts
@@ -233,9 +233,31 @@ export class CursorMoveCommands {
 		}
 	}
 
-	public static word(viewModel: IViewModel, cursor: CursorState, inSelectionMode: boolean, _position: IPosition): PartialCursorState {
+	public static word(viewModel: IViewModel, cursor: CursorState, inSelectionMode: boolean, _position: IPosition, _vimMouseCharacterModelRange?: Range): PartialCursorState {
 		const position = viewModel.model.validatePosition(_position);
-		return CursorState.fromModelState(WordOperations.word(viewModel.cursorConfig, viewModel.model, cursor.modelState, inSelectionMode, position));
+		const vimMouseCharacterModelRange = _vimMouseCharacterModelRange ? viewModel.model.validateRange(_vimMouseCharacterModelRange) : undefined;
+		return CursorState.fromModelState(WordOperations.word(viewModel.cursorConfig, viewModel.model, cursor.modelState, inSelectionMode, position, vimMouseCharacterModelRange));
+	}
+
+	private static vimMouseCharacterActivePosition(selectionStart: Range, range: Range): Position {
+		const targetStart = range.getStartPosition();
+		return targetStart.isBefore(selectionStart.getStartPosition()) ? targetStart : range.getEndPosition();
+	}
+
+	private static vimMouseCharacterSelectionStart(model: ICursorSimpleModel, cursor: CursorState): Range {
+		if (!cursor.modelState.selectionStart.isEmpty()) {
+			return cursor.modelState.selectionStart;
+		}
+
+		const position = cursor.modelState.selectionStart.getStartPosition();
+		const lineMaxColumn = model.getLineMaxColumn(position.lineNumber);
+		if (position.column < lineMaxColumn) {
+			return new Range(position.lineNumber, position.column, position.lineNumber, position.column + 1);
+		}
+		if (position.column > 1) {
+			return new Range(position.lineNumber, position.column - 1, position.lineNumber, position.column);
+		}
+		return cursor.modelState.selectionStart;
 	}
 
 	public static cancelSelection(viewModel: IViewModel, cursor: CursorState): PartialCursorState {
@@ -252,7 +274,7 @@ export class CursorMoveCommands {
 		));
 	}
 
-	public static moveTo(viewModel: IViewModel, cursor: CursorState, inSelectionMode: boolean, _position: IPosition, _viewPosition: IPosition | undefined): PartialCursorState {
+	public static moveTo(viewModel: IViewModel, cursor: CursorState, inSelectionMode: boolean, _position: IPosition, _viewPosition: IPosition | undefined, _vimMouseCharacterModelRange?: Range): PartialCursorState {
 		if (inSelectionMode) {
 			if (cursor.modelState.selectionStartKind === SelectionStartKind.Word) {
 				return this.word(viewModel, cursor, inSelectionMode, _position);
@@ -260,6 +282,18 @@ export class CursorMoveCommands {
 			if (cursor.modelState.selectionStartKind === SelectionStartKind.Line) {
 				return this.line(viewModel, cursor, inSelectionMode, _position, _viewPosition);
 			}
+			if (_vimMouseCharacterModelRange !== undefined) {
+				const vimMouseCharacterModelRange = viewModel.model.validateRange(_vimMouseCharacterModelRange);
+				const selectionStart = this.vimMouseCharacterSelectionStart(viewModel.model, cursor);
+				const activePosition = this.vimMouseCharacterActivePosition(selectionStart, vimMouseCharacterModelRange);
+				return CursorState.fromModelState(new SingleCursorState(
+					selectionStart,
+					SelectionStartKind.Simple,
+					0,
+					activePosition,
+					0
+				));
+			}
 		}
 		const position = viewModel.model.validatePosition(_position);
 		const viewPosition = (
diff --git a/src/vs/editor/common/cursor/cursorWordOperations.ts b/src/vs/editor/common/cursor/cursorWordOperations.ts
--- a/src/vs/editor/common/cursor/cursorWordOperations.ts
+++ b/src/vs/editor/common/cursor/cursorWordOperations.ts
@@ -738,7 +738,7 @@ export class WordOperations {
 		return null;
 	}
 
-	public static word(config: CursorConfiguration, model: ICursorSimpleModel, cursor: SingleCursorState, inSelectionMode: boolean, position: Position): SingleCursorState {
+	public static word(config: CursorConfiguration, model: ICursorSimpleModel, cursor: SingleCursorState, inSelectionMode: boolean, position: Position, vimMouseCharacterModelRange?: Range): SingleCursorState {
 		const wordSeparators = getMapForWordSeparators(config.wordSeparators, config.wordSegmenterLocales);
 		const prevWord = WordOperations._findPreviousWordOnLine(wordSeparators, model, position);
 		const nextWord = WordOperations._findNextWordOnLine(wordSeparators, model, position);
@@ -805,12 +805,20 @@ export class WordOperations {
 			column = cursor.selectionStart.endColumn;
 		} else if (position.isBeforeOrEqual(cursor.selectionStart.getStartPosition())) {
 			column = startColumn;
+			// Use the Vim mouse cell instead of the nearest native cursor boundary.
+			if (vimMouseCharacterModelRange && vimMouseCharacterModelRange.startLineNumber === lineNumber) {
+				column = Math.min(column, vimMouseCharacterModelRange.startColumn);
+			}
 			const possiblePosition = new Position(lineNumber, column);
 			if (cursor.selectionStart.containsPosition(possiblePosition)) {
 				column = cursor.selectionStart.endColumn;
 			}
 		} else {
 			column = endColumn;
+			// Use the Vim mouse cell instead of the nearest native cursor boundary.
+			if (vimMouseCharacterModelRange && vimMouseCharacterModelRange.endLineNumber === lineNumber) {
+				column = Math.max(column, vimMouseCharacterModelRange.endColumn);
+			}
 			const possiblePosition = new Position(lineNumber, column);
 			if (cursor.selectionStart.containsPosition(possiblePosition)) {
 				column = cursor.selectionStart.startColumn;
